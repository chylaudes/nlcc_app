<%= simple_form_for(@job) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :sunday_date %>
    <%= f.input :send_at %>
    <%= f.association :hotel %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
